events {}

http {
    server {
        listen 80;

        #server_frontend
        location / {
            proxy_pass http://frontend:80;
    }
        #server_backend
        location /api/ {
            proxy_pass http://backend:5000/;
        }
    }
}